V108 patch report

Applied final polish requested by the user:
- Renamed visible QBank mode from "Wizardry style" to "Wizary style" while preserving the internal value so old saved state keeps working.
- Kept "Traditional" as the immediate-feedback QBank mode.
- Removed the Profile Settings card from the settings grid.
- Improved the QBank Next question tap target so clicks on the center text are handled reliably.
- Added a Remind me button to mistake cards.
- Remind me opens a time picker with quick shortcuts. When the time is due, the mistake returns to the Home Refresh card.
- Renamed Home "Refresh & Find" card to "Refresh".
- Improved the Refresh card spacing, border, and visual polish to match the Today tasks card direction.
- Updated cache-busting and service worker version to V108.

Validation:
- All JavaScript files passed node --check.
- No data schema, Supabase table name, or localStorage key was intentionally changed.
