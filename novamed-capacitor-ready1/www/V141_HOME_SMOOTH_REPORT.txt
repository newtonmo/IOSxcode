NOVAMED v141 - Home Smoothness Patch

Base: v140 smooth cloud sync.

Changes:
1) Home speech / rotating motivational text now changes every 120 seconds instead of 9 seconds.
2) Prevented duplicate quote timers when the Home/Profile UI re-renders.
3) Quote rotation pauses while the browser tab is hidden.
4) Home smart widgets automatic refresh reduced from every 30 seconds to every 120 seconds.
5) Updated service worker/cache version to avoid stale Netlify/browser cache.

No SQL changes required.
Supabase cloud sync tables and v139/v140 functionality remain unchanged.
