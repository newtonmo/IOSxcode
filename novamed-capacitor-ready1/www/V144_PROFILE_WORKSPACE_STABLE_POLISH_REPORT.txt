NovaMed v144 - Profile workspace stable polish

Fixes:
- Prevents Profile tools from becoming unresponsive after opening nested modals from Bookmarks/Mistakes/Flashcards.
- Restores #adminPanel before any non-profile showModal replaces modal content.
- Avoids heavy hidden Profile panel re-renders; only the open Profile modal refreshes.
- Rebuilds adminPanel fallback if an older session destroyed it.
- Polishes Profile action cards to match the rest of the app design.

No SQL changes required.
