V110 Patch Report - Profile Modal Workspace

Goal:
- My Mistakes, Flashcards, and Bookmarks from Profile must open like the MCQ modal, not render under the Profile cards.

Changes:
1. Replaced Profile workspace behavior with a true modal workspace using #modalBackdrop.
2. My Mistakes, Flashcards, Bookmarks, Analytics, and Admin dashboard now mount inside the modal card.
3. The original adminPanel is moved into the modal while open, then restored to Profile on close. This preserves all existing IDs, state, handlers, and localStorage behavior.
4. Removed the under-page workspace issue that caused the Profile page to feel stuck/hang.
5. Added V110 modal styles to match the MCQ modal feel: centered, full workspace, scroll inside modal, hidden background.
6. Updated cache/version query strings to V110.

Verification:
- Ran node --check for all JavaScript files successfully.
- Verified no Arabic UI text remains in HTML/JS/CSS.
- No Supabase table names, localStorage keys, or QBank JSON schema were changed.
