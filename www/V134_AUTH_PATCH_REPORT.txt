NovaMed v134 Supabase Auth patch

Scope changed only parts related to Supabase connection, student authentication, and real Streak leaderboard.

Changed:
1. Updated embedded Supabase project config to:
   - https://qtxhqsteibgvcylywpmp.supabase.co
   - sb_publishable_T8G1YydoW0WF11rs4tgJjA_aDK_2ErO
2. getSupabaseConfig now prefers the embedded current URL/key over any old locally saved config, so an old browser config will not force the app to connect to the wrong project.
3. Added student-auth-cloud.js:
   - Student Sign in: email + password.
   - Student Sign up: email -> OTP -> first/second name -> password.
   - Uses Supabase Auth.
   - Saves profile/progress to profiles, student_state, and student_public.
   - Updates student_public for real Streak leaderboard.
4. Removed fake static Streak names from index.html and replaced them with a real-students-only placeholder.
5. Streak leaderboard now reads from student_public and shows only registered authenticated students.
6. Updated Supabase sync diagnostics to check profiles, student_state, and student_public instead of legacy student_profiles.
7. Updated script versions and service worker cache name to reduce old-cache issues.

Not changed:
- No design/style changes were made.
- No course/QBank/video logic was intentionally changed.
- Admin name/code login remains unchanged.
- Existing content table/storage logic remains unchanged.

Important deploy note:
If Netlify still shows the old app, unregister the old service worker or clear site data, then reload.
