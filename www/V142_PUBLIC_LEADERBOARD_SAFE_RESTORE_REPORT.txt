NOVAMED v142 - Public Leaderboard + Safe Cloud Restore

Base: v141 home smooth cloud sync.

Fixes included:
1. Streak/Street leaderboard now attempts to load real registered students even when the visitor is not signed in.
2. Added safe cloud high-water restore logic so a new device cannot overwrite an existing student's XP/streak with a fresh zero profile.
3. Signup/profile completion now merges with existing student_state/student_public rows instead of replacing them with new-device defaults.
4. Student progress saves now merge against the cloud baseline before writing. XP and streak use max/high-water protection.
5. Leaderboard cache increased to 2 minutes for smoothness.
6. Cache version updated to v142.

Important SQL:
Run SUPABASE_V142_PUBLIC_LEADERBOARD_SAFE_RESTORE.sql once in Supabase SQL Editor so guests can read visible leaderboard rows and existing students are backfilled into student_public.
