NovaMed v147 - إصلاح Auth وتنظيف RLS

1) تم حذف override الخاطئ simple-auth-rls-v146.js من التشغيل.
2) رجع Student Auth إلى النظام الصحيح:
   Sign Up: email -> OTP -> first name + second name + password
   Sign In: email + password
3) لم أغير وظائف QBank/Videos/Profile/Streak الموجودة؛ التعديل محصور بالـ auth runtime والكاش وملفات SQL.
4) تم تحديث service-worker/cache إلى v147 حتى لا تبقى نسخة v146 القديمة.
5) أضفت SQL جديد واضح:
   SUPABASE_V147_EMAIL_AUTH_FULL_RLS_CLEANUP.sql
   هذا ينشئ/يثبت كل جداول الطالب ويفعل RLS owner-only لكل جداول التقدم.
6) أضفت SQL اختياري فقط:
   SUPABASE_V147_OPTIONAL_ADMIN_CONTENT_LOCKDOWN.sql
   لا تشغله إلا بعد نقل Admin نفسه إلى Supabase Auth وتحديد role='admin' أو 'owner'.
7) لا تستخدم ملفات v146 الخاصة بالـ hidden Name+Code لأنها كانت السبب بتوحيد Sign In/Sign Up وخطأ تعطيل email confirmation.
